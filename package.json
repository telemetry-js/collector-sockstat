{
  "name": "@telemetry-js/collector-sockstat",
  "version": "0.0.3",
  "description": "Collect /proc/net/sockstat metrics (tcp only for now)",
  "license": "MIT",
  "author": "Vincent Weevers",
  "scripts": {
    "test": "standard && hallmark && node test"
  },
  "files": [
    "index.js",
    "lib"
  ],
  "dependencies": {
    "@telemetry-js/match-metric-names": "^0.0.3",
    "@telemetry-js/metric": "^0.1.2"
  },
  "devDependencies": {
    "hallmark": "^3.1.0",
    "standard": "^14.3.4",
    "tape": "^4.9.1"
  },
  "repository": "telemetry-js/collector-sockstat",
  "bugs": "https://github.com/telemetry-js/collector-sockstat/issues",
  "homepage": "https://github.com/telemetry-js/collector-sockstat",
  "keywords": [
    "metrics",
    "measurements",
    "telemetry"
  ],
  "engines": {
    "node": ">=6"
  }
}
